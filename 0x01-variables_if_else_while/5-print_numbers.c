#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Prints the numbers from 1-9
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
